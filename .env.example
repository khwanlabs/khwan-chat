# This sample needs NO environment variables and NO secrets.
#
# It runs entirely in the browser: you enter your API key, base URL, user id,
# and (optional) core in the app's settings panel, and they are stored in
# localStorage — never in a committed file and never on a server.
#
# There is nothing to configure here. This file exists only to document that.
